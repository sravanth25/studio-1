---
description: Start the local preview server for the Next.js application
---

# Preview Project Workflow

This workflow starts the Next.js development server to preview the project locally. 

1. **Start the Development Server**
// turbo
```bash
npm run dev
```

The application will be accessible at `http://localhost:3000`. You can test it by opening this URL in your web browser.
